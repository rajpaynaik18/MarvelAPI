<main class="bg-avengers">
    <div>
    <h1 class="h1-sg">{{title}}</h1>
    <img src={{image}}.{{extension}} alt="{{title}}" width="300" height="400" class="bg-img">
    <p class ="sc-p">{{description}}</p>
    <h2 class="comics">Comics</h2>
    <ul class="ul">
        {{#each comics}}
        <li>
            {{name}}    
        </li>
        {{/each}}
    </ul>
    </div>
</main>